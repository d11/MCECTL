# (expected answer is YES).

define N 6
define DEFAULT_INT_BITS N

init main;

error:	goto error;
ok:	goto ok;

module void main() {
	int a,x,c,i;

	if
	:: (a>0) -> x = 1;
	:: else -> break;
	fi;
	
	c = 0;
	
	i = 1;
loop:	do
	:: (i < 40) -> c = c+1; i = i+1;
	:: (i >=40) -> break;
	od;

	if
	:: (a > 0) -> if
		      :: (x == 0) -> goto error;
		      :: else -> goto ok;
		      fi;
	:: else -> goto ok;
	fi;
}
