define DEFAULT_INT_BITS 4

init main;

error:	goto error;
ok:	goto ok;

module void main()
{
	int X, Y;

	X = X * (X + 1);
	do
	:: (Y == (Y/2)*2) -> break;
	:: else           -> Y = Y + 1;
	od;
	if
	:: (X+Y == ((X+Y)/2)*2) -> goto ok;
	:: else -> goto error;
	fi;
}
