# exp.pds
define K 5		# size of array

bool b[K];

init main;

ok:	goto ok;
error:	goto error;


module void main() {

	bool a;

	a = true;	

	if
	:: (b[0]) -> skip;
	:: else	  -> skip;
	fi;

	if
	:: (b[1]) -> skip;
	:: else	  -> skip;
	fi;

	if
	:: (b[2]) -> skip;
	:: else	  -> skip;
	fi;

	if
	:: (b[3]) -> skip;
	:: else	  -> skip;
	fi;

	if
	:: (b[4]) -> skip;
	:: else	  -> skip;
	fi;


	if
	:: (a)  -> goto ok;
	:: else	-> goto error;
	fi;

}
	
