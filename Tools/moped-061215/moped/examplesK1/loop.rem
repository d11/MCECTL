define g 30
int l(9);

init main;

ok:	goto ok;
error:	goto error;


module void main() {
	int bit(1);
	l = 1; bit = 1;
	do
	:: (l >=g)	-> break;
	:: (l < g)	-> l = l+1;
	od;

	if
	:: (bit != 1)	-> goto error;
	:: else		-> goto ok;
	fi;

}
	
